using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

using Grasshopper;
using Grasshopper.Kernel;
using Grasshopper.Kernel.Data;
using Grasshopper.Kernel.Types;

namespace UnitTests
{
    [TestClass]
    public class Equalitites
    {
        [TestMethod]
        public void DataTrees()
        {
            var A = new GH_Structure<GH_Boolean>();
            
            var B = new GH_Structure<GH_Boolean>();

            // Same 
        }
    }
}
